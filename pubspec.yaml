name: td2_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # Pour les requêtes API dans card3.dart
  cupertino_icons: ^1.0.8 
  google_fonts: ^6.3.2 # Pour le thème dans mytheme.dart
  
  
  # Dépendances ajoutées pour le TD
  settings_ui: ^2.0.2 # Pour l'écran de paramètres dans card4.dart
  shared_preferences: ^2.2.3 # Pour la persistance du thème
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/tasks.json
